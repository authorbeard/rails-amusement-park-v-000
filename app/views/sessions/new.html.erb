<%= form_for User.new, :url=>sessions_create_path do |f| %>
    <%= f.label :name, "Select your name" %><br>
    <%= f.collection_select :id, User.all, :id, :name, allow_blank: :true %><br>

    <%= f.submit "Sign In" %>

<% end %>
